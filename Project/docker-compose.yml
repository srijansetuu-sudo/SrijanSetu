services:
  postgres:
    image: postgres:16
    container_name: srijansetu_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: srijansetu
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - srijansetu_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d srijansetu"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  srijansetu_postgres_data:

